Trilloka Architect Engine V6.3 — tiered paid access + owner/admin control validation

Python compile: PASS
Full regression/access/admin suite: 50 passed

Scanner/report foundation retained:
- original V6 scanner/scorer/checkpoint/report regression suite remains green
- revenue-first ranking and family deduplication retained
- 50-checkpoint evidence engine retained
- free/paid visibility is a wrapper around the same V6 scan result

Commercial access behavior tested:
- free preview: 1 successful scan per IP/device per rolling 24 hours
- failed free scan does not consume allowance
- paid entitlement requires matching email + purchased domain + secure purchase pass
- Essential $350: 2 successful fresh scans/day, Top 3 detailed remediation, full 50-check data
- Advanced $550: 3 successful fresh scans/day, Top 6 detailed remediation, full 50-check data, 1 guidance call
- Architect $850: 4 successful fresh scans/day, Top 10 detailed remediation, full 50-check data, 2 guidance calls, 15-hour email response target
- failed paid scan does not consume daily allowance
- free preview strips full 50-check data and detailed patch instructions
- paid report visibility is exactly Top 3 / Top 6 / Top 10 according to tier
- domain cache behavior retained

Owner/admin controls tested:
- admin key is required for private owner endpoints
- activate paid plan after verified purchase
- complimentary/custom-duration plan activation
- upgrade/downgrade tier without rotating customer pass
- extend/shorten expiry and set exact expiry
- revoke customer access immediately
- restore customer access
- rotate lost/compromised pass; previous pass becomes invalid
- change purchased domain while preserving paid usage history
- reset today's scan usage without deleting history (rows become admin_reset)
- record/undo included guidance-call usage
- list entitlements without raw customer email
- admin route set is present
- admin API lifecycle authorization/activation/update/revoke tested through FastAPI TestClient

Privacy/security checks:
- raw customer email is not stored in access SQLite database
- raw client IP is not stored in access SQLite database
- plaintext purchase pass is not stored; only HMAC hash is retained
- customer plan status uses POST body rather than query string for purchase pass
- admin API secret is server-side header only

Core public routes retained:
  POST /api/audit
  POST /api/scan
  GET  /health
  GET  /diagnostic
  GET  /api/plans

Customer plan route:
  POST /api/plan/status

Private owner routes:
  POST /api/admin/activate-plan
  POST /api/admin/grant-scan-package
  GET  /api/admin/scan-usage
  GET  /api/admin/entitlements
  POST /api/admin/entitlement/status
  POST /api/admin/entitlement/update
  POST /api/admin/entitlement/revoke
  POST /api/admin/entitlement/restore
  POST /api/admin/entitlement/rotate-pass
  POST /api/admin/entitlement/change-domain
  POST /api/admin/entitlement/reset-daily-usage
  POST /api/admin/entitlement/guidance-call

Operational requirements:
- SCAN_ACCESS_DB_PATH must use persistent storage.
- TRILLOKA_ADMIN_API_KEY must be a long random server-side secret.
- SCAN_ACCESS_SECRET must remain stable across deployments.
- TRUST_PROXY_HEADERS=true only behind a proxy/CDN that sanitizes forwarded client-IP headers.
- For multi-replica horizontal scaling, move access state from per-replica SQLite to a shared transactional database/store.
- Payment verification remains external. A trusted server/webhook should call /api/admin/activate-plan only after payment settlement is verified.
