Trilloka Architect Engine V6.4 — locked owner email OTP admin validation

Python compile: PASS
Full regression/access/admin/auth suite: 57 passed

V6 engine integrity:
- hybrid_scanner.py: byte-for-byte identical to clean V6
- checkpoint_engine.py: byte-for-byte identical to clean V6
- scorer.py: byte-for-byte identical to clean V6
- report_engine.py: byte-for-byte identical to clean V6
- behavioural_engine.py: byte-for-byte identical to clean V6
- scraper.py: byte-for-byte identical to clean V6

Commercial access behavior retained:
- free preview limit and cache behavior retained
- $350 Essential: Top 3, 2 successful fresh scans/day, full 50-check data
- $550 Advanced: Top 6, 3 successful fresh scans/day, full 50-check data, 1 guidance call
- $850 Architect: Top 10, 4 successful fresh scans/day, full 50-check data, 2 guidance calls, 15-hour response target
- failed scans do not consume allowance
- paid access remains email + purchased-domain + secure-pass bound

Owner OTP behavior tested:
- owner address locked to onlyonearpit@gmail.com
- build rejects configuration attempting to redirect OTP to another email
- OTP is not stored in plaintext
- owner email is not stored in auth database
- OTP is single-use
- OTP requires the browser challenge that requested it
- OTP request is network-bound by default
- admin session validation/revocation works
- protected admin API lifecycle works using OTP session cookie
- logout removes owner authorization
- /docs, /redoc and /openapi.json are not public

Owner security defaults:
- code TTL: 30 minutes
- owner session TTL: 15 minutes
- max wrong attempts: 5
- request cooldown: 60 seconds in production example
- per-requester and global hourly request limits
- Secure + HttpOnly + SameSite=Strict owner session/challenge cookies
- legacy human admin API-key access disabled by default
- separate machine-to-machine plan activation key supported for verified payment webhooks

Core routes retained:
  POST /api/audit
  POST /api/scan
  GET  /health
  GET  /diagnostic
  GET  /api/plans
  POST /api/plan/status

Private owner entry point:
  GET /admin

Owner auth routes:
  POST /api/admin/auth/request-code
  POST /api/admin/auth/verify-code
  GET  /api/admin/auth/status
  POST /api/admin/auth/logout
